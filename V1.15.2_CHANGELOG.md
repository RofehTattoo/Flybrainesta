## FIX2 — official motor subclass completion + independent semantic validation

The first pandas-free CI run successfully opened the official Feather and
reached the actual annotation semantics. It stopped at bodyId 164190 because
the official `class` field is blank for MNad21 although its measured `subclass`
is `ad`.

This revision:
- derives motor anatomical class from the measured MaleCNS subclass vocabulary;
- requires populated `class` and `subclass` to agree;
- records blank-class completion explicitly as `classSource=subclass_completion`;
- independently re-reads the official Feather in the validator and compares
  all 708 rows field-by-field;
- validates the exact TSV schema in Android;
- fixes a HALTERE telemetry counter that was accidentally accumulating across
  simulation ticks;
- pins the CI Feather reader to `pyarrow==19.0.1`.

No FBC103, FBD104, neuron count, edge count, structural weight, or neural
dynamics rule is changed.

## FIX — pandas-free official Feather reader

The VNC semantics builder previously called `pyarrow.feather.read_feather()`.
That API converts the Feather table through pandas, causing CI to fail because
pandas was not installed. The builder now uses `pyarrow.feather.read_table()`
and Arrow-native `to_pylist()` conversion. No scientific/model data or frozen
connectome artifact is changed.

# FlyBrain V1.15.2 — VNC Motor Semantics

- Added `tools/build_vnc_motor_semantics.py`, using the exact official MaleCNS v1.0 annotation Feather.
- Added `tools/validate_vnc_motor_semantics.py` with VNC-01..06 gates.
- Added generated runtime asset `vnc_motor_semantics.tsv` (created by CI from the pinned official source).
- Runtime anatomical classes are LEG/WING/ABDOMEN/NECK/HALTERE/OTHER.
- Removed JUMP as an anatomical class; TTMn is WING with `functionalTag=JUMP`.
- Official motor side is loaded from the semantic map for the VNC body layer.
- FBC103 remains byte-for-byte frozen; its legacy role byte is retained only for provenance/audit comparison.
- FBD104 remains untouched by this layer.
- OTHER motor output remains counted/observable and is not silently converted to zero.
- No stimulus identity is injected into body mechanics.

## Gate status

The source-side structural and semantic implementation is complete. Full 708/708 generation and APK compilation require CI because the official Feather must be read with `pyarrow`; the local environment does not contain Arrow/Gradle tooling. The workflow performs the exact SHA-pinned generation and validation before compilation.


### FIX3 — VNC census bug
The VNC semantics builder now counts and emits the effective anatomical class (`anatomicalClass` / completed `class`) rather than the nullable raw MaleCNS `class` field. This prevents the valid all-blank raw `class` field from producing a false `{'': 708}` census. `classSource` preserves whether the value came directly from `class` or was completed from the official motor `subclass` vocabulary. No FBC103/FBD104 data are changed.
