# V1.15.2 FIX7 — Builder Runtime Audit

## Failure fixed
CI reached the official Feather reader and failed in `build_vnc_motor_semantics.py` with:
`NameError: name 'clean' is not defined`.

The validator already defined `clean()`, but the builder did not. The builder called `clean()` while filtering the official `superclass` and reading semantic fields.

## Correction
Added one local, pandas-free `clean()` normalization helper to the builder. It accepts `None`, PyArrow scalar values (`as_py()`), and ordinary Python values.

## Scientific scope
No changes to FBC103, FBD104, the FBR-10 graph, neuron count, edges, weights, or motor semantic rules.

## Next CI gate
The workflow should now execute the full builder, then the independent validator. Do not interpret this fix as a scientific PASS; it only removes the runtime NameError.
